name = "summerpark-backend"
main = "index.js"
compatibility_date = "2024-01-01"

# Cloudflare D1 資料庫綁定設定
# 使用前請先透過 npx wrangler d1 create summerpark-db 建立資料庫，並將 database_id 填入下方
[[d1_databases]]
binding = "DB"
database_name = "summerpark-db"
database_id = "YOUR_DATABASE_UUID_HERE"
