al       al

section, 每

區塊至少佔滿 100vh, tet
卡片化與網格 (Card & Grid): 內部面板使用 Flexbox 與 Grid 佈局(如
Tfr))), 確保從手機到 4K 螢幕的流

grid-template-columns: repeat(auto-fit, minmax(360px,

暢縮放。

2. Section 2: Battery Monitor FEMERE AR, Bites

資訊層級: 1. Section 1: Control Panel (即時狀態總覽,音

類似儀表板的專注體驗。

度)。

3. Section 3: USB & Ports Monitor (外接裝置測試、 讀寫速率)。

以下功能需將 Prototype 的
4.1 核心系統與顯示測試
功能模組

System info

Storage

Monitor

Touch

4.2 能源與環境測試

功能模組

Battery

25 pI

Fr fie

說明

顯示CPU SRR ID BL. BR
記憶體(RAM)容量等級、
GPU 渲染器型號及裝置
User-Agent.

顯示瀏覽器可用之儲存配額
與已用空間評估。

檢測螢幕解析度、可用工作
區,DPR (像素比例) 與色彩
深度。

測試螢幕支援的多點人觸控數

Bo

說明

監測即時電量百分比、充電
狀態(充/放電)、預估剩餘時
間,。 觸發[充放電測試」時記

求規格 (Functional Requirements)
介面綁定至瀏覽器原生的 Web APL HGR BAA SHE.

對應 Web API

navigator.hardwareConcurr
ency,
navigator.deviceMemory,
WebGLContext.getExtensio
n(‘WEBGL_debug_renderer
_info’)

navigator.storage.estimate(

)

window.screen.width/heigh
t, window.devicePixelRatio,
screen.colorDepth

navigator.maxTouchPoints,
搭配 Canvas 監聽
touchstart/touchmove 繪製

對應 Web API

navigatorgetBattery() (監聽
levelchange,
chargingchange)
